# 60-second talk track (Borealis AI Data Analyst)

**Open:** `outputs/adoption_dashboard.html` while you speak.

1. **Problem:** Enablement teams get seat counts and session volume. That hides whether GenAI is *used* or *valuable*.

2. **What I built:** Synthetic warehouse — 420 licensed users, phased BU rollout, event log with a **value-event** flag (completed task vs toy session).

3. **Findings (recite from readout):**
   - **Draft** ~22% value rate vs **search ~99%** — don’t put draft on the exec slide.
   - **Retail** slowest time-to-first-value (~25 days) + highest copy-to-external — enablement + policy, not model swap.
   - **Huddle vs webinar:** huddle wins on 14-day TTV (see experiment memo) — default huddle in lagging BUs.

4. **Cadence:** Six SQL questions → weekly one-pager → measurement playbook for post-go-live.

5. **Close:** Same skills on real data: Postgres/SQL Server, Power BI, Python, talking to users after rollout.

**If they ask GenAI:** GrantPath = shipped GPT workflow; this project = *measuring* adoption and quality of enablement.

**If they ask Borealis research:** This role is **AI Business Enablement** in T&O (dashboards + rollouts), not time-series forecasting — I’m applying to the enablement analyst posting (R-0000184505).
